import { Html } from "@react-three/drei";

const Loader = () => {
  return (
    <Html center>
      <p className="text-[#aaa] font-semibold">Loading...</p>
    </Html>
  );
};

export default Loader;
